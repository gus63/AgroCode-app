version: '3.5'

services:
  db_auth:
    container_name: postgresql
    ports:
      - "5432:5432"
    environment:
    - POSTGRES_PASSWORD=123
    - POSTGRES_USER=postgres
    image: postgres:14.3-alpine